spring:
  application:
    name: assignment-service
  data:
    mongodb:
      uri: mongodb+srv://kmnavoddilshan:EfxVFWxNLKmA4Pwk@cluster0.cdlujqw.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: questions
  config:
    import: "optional:configserver:http://localhost:8088"

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000
        loggerLevel: basic